import { useEffect,useState } from 'react';import Button from '../Button.jsx';import { assetUrl } from '../../utils/image.js';
const blank={customer_name:'',vehicle:'',delivery_date:new Date().toISOString().slice(0,10),story:'',visible:true,is_demo:false};
export default function DeliveryForm({initial,onSubmit,busy}){const[f,setF]=useState(blank),[file,setFile]=useState(null);useEffect(()=>{setF(initial?{...blank,...initial,delivery_date:(initial.delivery_date||'').slice(0,10)}:blank);setFile(null)},[initial]);const ch=e=>setF(x=>({...x,[e.target.name]:e.target.type==='checkbox'?e.target.checked:e.target.value}));const submit=e=>{e.preventDefault();const data=new FormData();Object.entries(f).forEach(([k,v])=>data.append(k,v));if(file)data.append('image',file);onSubmit(data)};return <form onSubmit={submit} className="grid gap-4 sm:grid-cols-2"><Field label="Customer name" name="customer_name" value={f.customer_name} onChange={ch}/><Field label="Vehicle" name="vehicle" value={f.vehicle} onChange={ch}/><Field label="Delivery date" type="date" name="delivery_date" value={f.delivery_date} onChange={ch}/><div className="sm:col-span-2"><label className="label">Delivery photo</label>{initial?.image&&<img src={assetUrl(initial.image)} alt="Delivery" className="mb-3 h-32 rounded-xl object-cover"/>}<input className="input" type="file" accept="image/jpeg,image/png,image/webp" onChange={e=>setFile(e.target.files[0]||null)}/></div><div className="sm:col-span-2"><label className="label">Story</label><textarea className="input min-h-28" name="story" value={f.story} onChange={ch} required/></div><Check name="visible" checked={f.visible} onChange={ch} label="Visible"/><Check name="is_demo" checked={f.is_demo} onChange={ch} label="Mark as demo"/><div className="sm:col-span-2"><Button type="submit" variant="red" disabled={busy}>{busy?'Saving...':'Save Delivery'}</Button></div></form>}
function Field({label,...props}){return <div><label className="label">{label}</label><input className="input" required {...props}/></div>}function Check({label,...props}){return <label className="flex items-center gap-2 text-sm font-semibold"><input type="checkbox" {...props}/>{label}</label>}
