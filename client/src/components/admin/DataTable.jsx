import EmptyState from '../EmptyState.jsx';
export default function DataTable({columns,rows,renderActions}){ if(!rows?.length)return <EmptyState title="No records found"/>; return <div className="overflow-x-auto rounded-2xl border border-zinc-200 bg-white"><table className="min-w-full text-sm"><thead className="bg-zinc-50 text-left text-xs uppercase tracking-[.12em] text-zinc-500"><tr>{columns.map(c=><th key={c.key} className="px-4 py-3">{c.label}</th>)}{renderActions&&<th className="px-4 py-3">Actions</th>}</tr></thead><tbody className="divide-y divide-zinc-100">{rows.map(row=><tr key={row._id} className="align-top">{columns.map(c=><td key={c.key} className="px-4 py-4 text-zinc-700">{c.render?c.render(row[c.key],row):String(row[c.key] ?? '—')}</td>)}{renderActions&&<td className="px-4 py-4">{renderActions(row)}</td>}</tr>)}</tbody></table></div>; }
