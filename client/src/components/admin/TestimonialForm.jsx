import { useEffect,useState } from 'react';import Button from '../Button.jsx';import StarRatingInput from '../StarRatingInput.jsx';
const blank={customer_name:'',vehicle:'',rating:5,testimonial:'',approved:false,featured:false,is_demo:false};
export default function TestimonialForm({initial,onSubmit,busy}){const[f,setF]=useState(blank);useEffect(()=>setF(initial?{...blank,...initial}:blank),[initial]);const ch=e=>setF(x=>({...x,[e.target.name]:e.target.type==='checkbox'?e.target.checked:e.target.value}));const submit=e=>{e.preventDefault();const data=new FormData();Object.entries(f).forEach(([k,v])=>data.append(k,k==='rating'?Number(v):v));onSubmit(data)};return <form onSubmit={submit} className="grid gap-4 sm:grid-cols-2"><Field label="Customer name" name="customer_name" value={f.customer_name} onChange={ch}/><Field label="Vehicle" name="vehicle" value={f.vehicle} onChange={ch}/><StarRatingInput value={f.rating} onChange={rating=>setF(x=>({...x,rating}))}/><div className="sm:col-span-2"><label className="label">Testimonial</label><textarea className="input min-h-28" name="testimonial" value={f.testimonial} onChange={ch} required/></div><Check name="approved" checked={f.approved} onChange={ch} label="Approved"/><Check name="featured" checked={f.featured} onChange={ch} label="Featured"/><Check name="is_demo" checked={f.is_demo} onChange={ch} label="Mark as demo"/><div className="sm:col-span-2"><Button type="submit" variant="red" disabled={busy}>{busy?'Saving...':'Save Testimonial'}</Button></div></form>}
function Field({label,...props}){return <div><label className="label">{label}</label><input className="input" required {...props}/></div>}function Check({label,...props}){return <label className="flex items-center gap-2 text-sm font-semibold"><input type="checkbox" {...props}/>{label}</label>}
