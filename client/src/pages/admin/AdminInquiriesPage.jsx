import { useEffect,useState } from 'react';import InquiryTable from '../../components/admin/InquiryTable.jsx';import LoadingSpinner from '../../components/LoadingSpinner.jsx';import ErrorState from '../../components/ErrorState.jsx';import{listResource,updateInquiryStatus}from'../../services/adminService.js';
export default function AdminInquiriesPage(){const[items,setItems]=useState([]),[loading,setLoading]=useState(true),[error,setError]=useState(''),[status,setStatus]=useState(''),[type,setType]=useState(''),[search,setSearch]=useState('');const load=()=>{setLoading(true);setError('');listResource('inquiries',{status:status||undefined,type:type||undefined,search:search||undefined}).then(setItems).catch(e=>setError(e.response?.data?.message||'Unable to load inquiries.')).finally(()=>setLoading(false))};useEffect(()=>{const t=setTimeout(load,200);return()=>clearTimeout(t)},[status,type,search]);const change=async(id,next)=>{try{await updateInquiryStatus(id,next);setItems(xs=>xs.map(x=>x._id===id?{...x,status:next}:x));}catch(e){setError(e.response?.data?.message||'Unable to update status.')}};return <div><p className="eyebrow">Lead management</p><h1 className="mt-2 text-3xl font-black">Inquiries</h1><div className="mt-6 grid gap-3 rounded-2xl border border-zinc-200 bg-white p-4 sm:grid-cols-3"><input className="input" placeholder="Search name, email, vehicle" value={search} onChange={e=>setSearch(e.target.value)}/><select className="input" value={status} onChange={e=>setStatus(e.target.value)}><option value="">All statuses</option>{['New','Contacted','Closed'].map(x=><option key={x}>{x}</option>)}</select><select className="input" value={type} onChange={e=>setType(e.target.value)}><option value="">All inquiry types</option>{['Vehicle Inquiry','Request a Quote','Test Drive','Financing Inquiry','General Inquiry'].map(x=><option key={x}>{x}</option>)}</select></div>{error&&<div className="mt-5"><ErrorState message={error}/></div>}<div className="mt-5">{loading?<LoadingSpinner/>:<InquiryTable items={items} onStatusChange={change}/>}</div></div>}
